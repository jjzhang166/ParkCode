1、车牌宽度：（单位：像素）
	根据现场需要进行设置，与摄像机焦距、车速等相关。宽度范围越大，识别的景深区域越大，此时同一个车牌识别出的结果数量可能增加，（可用下面的识别时间和可信度过滤错误车牌）。

2、省份优先级：建议设置为检测车辆最多的省份缩写

3、最多输出车牌个数：同一帧视频中车牌个数。（与车道数量相关）

4、视频定位阀值：建议采用默认值7

5、视频识别阀值：影响CPU资源占用，根据计算机配置调整。默认值5

6、图像定位阀值：建议采用默认值5

7、图像识别阀值：建议采用默认值1

8、视频中每个车牌只输出一次：建议勾选（只输出可信度最高的结果）

9、隔行抽点：如果车牌图片宽高比较小时候勾选

10、输入场图像：如果车牌图片宽高比较大时候勾选

11、识别区域：全部0为识别整个画面中的车牌

12、识别区域设置：限定车牌识别区域(单位：像素，左右0-704，上下0-576，全0识别整个画面中的车牌)

13、移动侦测延时：检测到移动物体后开启识别，所设置时间为全画面无移动后停止识别的延时，建议车辆密集、速度较快时适当延长时间。（启动车牌识别需要时间）

14、车牌可信度：过滤可信度低于设置值的结果输出。

15、识别时间：过滤识别时间超过设置值的结果输出。

16、采集卡：多张视频采集卡配置。